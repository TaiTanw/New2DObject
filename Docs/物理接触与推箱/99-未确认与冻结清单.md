# 99 — 未确认项与冻结清单

> 动手实现前扫一眼；确认后把条目改成「已确认」并回写到对应文档。  
> 上级目录：[README.md](./README.md)

---

## A. 未确认 / 模糊（待核对）

| ID | 问题 | 备注 | 状态 |
|----|------|------|------|
| U1 | 小速度「不施力」、大速度才下帧施力的 **相对法向速度阈值** | 数值未定 | 未确认 |
| U2 | `positionalEmbedding` 语义：只表示预测重叠，还是同时保留帧初 Distance？ | 建议拆字段命名 | 未确认 |
| U3 | 静墙向内速度削弱：是否 **必须本帧**（文档 03 推荐本帧） | 若坚持「仅位移后确认」需接受一帧挤入 | 倾向本帧，待你拍板 |
| U4 | 多箱链 MVP：是否固定 **每帧只传一跳（N=1）** | 文档 02 默认是 | 倾向是，待你拍板 |
| U5 | 可推 pair 的 broadphase：全量 O(n²) 是否 MVP 可接受 | 箱少时可接受 | 未确认 |
| U6 | 「正在推」表现布尔：仅 pair 存活，还是 `bias` 模长阈值？ | 动画用 | 未确认 |
| U7 | 静墙裁剪与可推 bias **同时作用在同一实体**时的合成顺序 | 建议先可推累加 bias，再静墙法向夹紧 | 未确认 |
| U8 | 旧相位 6 `SecondOrderPhyFun` 推箱路径删除时机 | 新相位 4 解算可用后再删 | 未确认 |
| U9 | `PhysicalBoundingBox.size` 为半伸长还是全宽？ | 须与 `v3=a.size+b.size` 重叠公式一致 | 未确认 |
| U10 | `displacementBias` 升为 `Vector2`（或按轴字段）？ | 现为 `float`，轴向挤出不够用 | 未确认 |
| U11 | 无 `ICanMove` 的箱子之间是否永远只靠 bias、不做速度施力？ | 与「移动能力门控」绑定 | 未确认 |
| U12 | 接触冲量允许计入的被动速度集合（限时 / 哪些 `E_PhyForceType`） | 注释倾向排除状态速度 | 未确认 |

---

## B. 已冻结（不要再展开）

| ID | 项 |
|----|-----|
| F1 | 不做完整 `PhysicsScene2D` 第二物理世界 |
| F2 | 不做同帧力→预测→力的无界递归 |
| F3 | 斜坡/旋转预测后置 |
| F4 | 不用动态力互挂表达推箱反作用 |
| F5 | 不取消相位屏障却假装多体公平 |
| F6 | **静态墙不进** 预测 AABB pair 世界 |
| F7 | 可推实体间速度施力默认 **T+1**；bias 必须本帧 |
| F8 | 多层挤出默认 **N=1 冻快照累加**；禁止无界挤到分离 |
| F9 | 实体接触 **速度施力** 以 `ICanMove` 为门控（非相对速度阈值主判据） |
| F10 | 接触速度意图进 `secondaryPressureDic`（或等价专属容器），**T+1** 入账；状态速度不作为瞬间冲量参数 |

---

## C. 与旧开放列表的合并说明

原「01」文末开放项已迁入上表 U1–U4；静墙相关结论见 [03](./03-静态墙与预测世界边界.md)，并从「静墙是否本帧 bias」改为「静墙不走 pair，本帧法向裁剪」。

---

## 相关

- [目录](./README.md)  
