# 99 — 未确认项与冻结清单

> 上级目录：[README.md](./README.md)
> 2026-09 落地后已按实现回写。

---

## A. 未确认 / 模糊

| ID | 问题 | 备注 | 状态 |
|----|------|------|------|
| U1 | 相对法向速度阈值分档 | 水平相对闭合语义已落地，当前阈值 `0.01`，高低速分档仍未定标 | **部分完成，待 P0 实测** |
| U2 | `positionalEmbedding` 语义 | 管线用解算器内 v4；该字段闲置 | **确认闲置** |
| U3 | 静墙向内速度削弱是否本帧 | 静墙仍本帧裁剪 `wordDelta` + 清向内时间力 | **已确认本帧** |
| U4 | 多箱 N=1 | 斜坡双箱已复现失败；需升级为解算器内迭代约束 | **已拍板处理** |
| U5 | O(n²) broadphase | 箱少 MVP | **已接受** |
| U6 | 「正在推」表现数据 | 已确认不使用单一动画布尔；需公开接触事实并由表现层解析 | **策略已拍板，代码待 P0 后实施；见 07** |
| U7 | 静墙裁剪与 bias 合成顺序 | 先加 bias，再静墙夹紧 x | **已确认** |
| U8 | 删除 `SecondOrderPhyFun` | 已删除推墙 AddForce | **已完成** |
| U9 | `size` 半宽还是全宽 | `bounds.extents`，半长宽 | **已确认半宽** |
| U10 | bias 升 Vector2 | `EntitySolutionResult.displacementOffset` 为 Vector2 | **已确认** |
| U11 | 箱–箱速度传播 | 已移除 `ICanMove` 门控并改为有向动态力；N=1 同帧链仍未闭环 | **部分完成，待多箱迭代** |
| U12 | 冲量计入哪些被动速度 | 用平面总速度（主动+被动）；未单独剔除状态速度 | **MVP 已确认** |
| U13 | EPhy 实体是否全 Kinematic | 当前 Dynamic 混合模式稳定；全 Kinematic 尚缺静态扫掠/残嵌恢复 | **重新开放** |
| U14 | Skin / slop / 接触保持 | `slop=0.002` 已落地；skin、候选释放阈值和稳定接触事实仍未实现 | **P0 待处理** |
| U15 | 静墙裁剪如何反馈实体 pair | 当前墙后裁剪会使前面的双方偏置失效 | **待设计** |
| U16 | `wall (2)` 墙顶边缘起跳停顿 | Ground/Wall 掩码均含 Layer 6+7，distance=0 命中分类可能切换 | **待原始命中记录确认** |
| U17 | 持续推箱仍有轻微抖动 | 可能来自 pair/link 反复开关、速度越界或 Unity Dynamic 二次修正 | **当前 P0；见 06 §1.1** |
| U18 | 箱体同方向自由度/阻塞推动 | 相位 4 实体接触早于相位 5 静墙裁剪，管理器无法完整判断目标最终能否移动 | **待方向约束或 appliedDelta 结果接口** |

仍开放或待实测：**U1、U6、U13–U18**；当前执行优先级以 U17 为 P0。

---

## B. 已冻结

| ID | 项 |
|----|-----|
| F1 | 不做 `PhysicsScene2D` 第二世界 |
| F2 | 不做同帧力→预测递归 |
| F3 | 斜坡/旋转预测后置 |
| F4 | 不恢复实体相位 5 的单边施力引用；有向动态力关系由管理器统一维护 |
| F5 | 保留相位屏障 |
| F6 | 静态墙不进预测 AABB pair |
| F7 | 速度施力 T+1；bias 本帧 |
| F10 | 接触速度 T+1 走 `dynamicForceDic/ForceData.speedStacking`；`EntitySolutionResult` 不保存跨帧速度 |
| F11 | 调试工具只读观测，不注册物理时序、不回写运行时解算数据 |

---

## 相关

- [目录](./README.md) · [04 实现对照](./04-实体接触速度算法.md) · [06 已拍板事务](./06-已拍板事务与开工顺序.md) · [07 推箱表现策略](./07-推箱交互事实与表现状态.md)
