# 08 — 第一阶段：运动快照、环境接入与唯一环境入口

本文汇总第一阶段 F_5.1、F_5.2、F_5.3 的已实施结果。F_5.1 统一预测与提交的基础位移；F_5.2 整理环境能力与登记关系；F_5.3 收敛为单环境宿主、多能力接口、单脚下来源。接触生命周期、速度回写及多箱迭代仍属于后续工作。

## 1. 本次修改解决什么

原相位 3 用 `GetPlanarVelocity() * dt` 预测 AABB，相位 5 才进行地面切线处理和平台位移补偿。斜坡或移动平台上，两次计算使用的基础位移不同。

现在由实体在相位 3 构建一次 `EntityMotionFrame`，预测和提交复用其中的 `plannedWorldDelta`。原相位 5 的地面位移公式原样提取，接触力算法、静墙裁剪和 Dynamic 刚体模式保持现有实现。

改变的是预测所依据的基础运动。相同速度、地面及平台输入下，基础提交位移保持原规则；由于接触候选位置改变，实际接触偏置及随后运动结果可能随之变化。

## 2. 职能与数据归属

| 结构/方法 | 职能及生命周期 | 本次变化 |
|---|---|---|
| `PlayerPhysicsData` | 实体已有速度状态，由速度阶段和事件处理写入 | 字段及入账方式不变 |
| `EntityMotionFrame` | 实体持有的本帧未约束运动快照；相位 3 整体替换 | 只读值结构；正式运行保存 `plannedWorldDelta`，其余分量作为 EditorOnly 观测副本 |
| `BasicEntity.BuildMotionFrame` | 将已采样的速度、几何、平台及步长转换为快照 | 新增纯计算方法，从原相位 5 提取地面位移公式；不另行积分力或写 Unity 世界 |
| `PhysicalBoundingBox` | 向管理器上报预测中心、半长宽和实体引用 | 字段不变；中心改为当前 Collider 中心加 `plannedWorldDelta` |
| `EntitySolutionResult` | 相位 4 累加、本帧相位 5 消费的接触纠偏 | 仍只有 `displacementOffset`，不保存基础运动或跨帧速度 |
| `EntityPhysicsDebugSnapshot` | 编辑器只读观测结果 | 基础运动字段改从运行时运动快照复制，新增 `plannedWorldDelta` |
| `PlayerDataWindow` | 展示实体提供的快照 | 新增“预测/提交共用基础位移”，不参与运行时计算 |

`EntityMotionFrame` 放在现有 `Physics/Data/Struct.cs`，本轮没有迁移序列化字段或新增场景组件。

快照字段的区别：

| 字段 | 含义 |
|---|---|
| `debugFreeVelocity` | EditorOnly：速度结算后的原始合量，尚未应用地面位移规则 |
| `debugIntegratedVelocityDelta` | EditorOnly：原始 `freeVelocity * fixedDeltaTime` 的副本 |
| `debugMotionDelta` | EditorOnly：按原有地面规则处理后的实体位移副本，不含平台 |
| `debugPlatformDelta` | EditorOnly：本帧着地平台位移的副本；离地时为零 |
| `plannedWorldDelta` | `motionDelta + platformDelta`，在构造时合成一次 |

这些字段不是可以全部相加的速度/位移来源。速度积分和地面处理表达前后两种阶段；构建方法中的运行时局部量 `motionDelta + platformDelta` 组成基础位移，四个 debug 字段只供解释来源。格式约定见 [05 §4.1](./05-数据语义与调试约定.md#41-editoronly-命名与代码格式)。

## 3. 本帧调用顺序

```text
相位 1：GeometricQuery
相位 2：PhyFunUpdate
相位 3：SpeedCalculation
          → 原有水平/竖直速度计算（含上一帧接触动态力）
          → 采样地面法线、着地状态、fixedDeltaTime；读取相位 2 环境帧中的平台 Delta
          → BuildMotionFrame，整体替换实体 motionFrame
          → PositionPrediction
              清零本帧接触偏置
              Collider 中心 + motionFrame.plannedWorldDelta → 预测 AABB
相位 4：ContactForSolution
          → 本帧 displacementOffset
          → 原有下一帧接触动态力
相位 5：DisplacementCorrection
          → motionFrame.plannedWorldDelta + displacementOffset
          → 原有静墙裁剪
          → 刷新已解析的墙滑引用、记录编辑器观测值
          → 一次 MovePosition
下一帧：采集实际位置及 engineCorrection
```

相位 5 不再重新计算速度积分、斜坡切线或平台补偿；平台位移因此不会在预测和提交时重复相加。

`MovePosition` 保持为相位 5 回调的最后一条语句。墙滑引用来自此前已解析的静墙状态，更新放在提交前，不依赖提交后的真实几何；Unity 对移动请求的物理执行仍发生在后续模拟中。

“预测与提交一致”指二者的基础位移相同。接触纠偏、静墙裁剪和 Unity 后续修正仍会让最终落点不同于最初预测，不能要求二者最终位置始终相等。

现有水平接触施力仍读取 `GetPlanarVelocity()`，没有改为 `plannedWorldDelta / dt`。本轮没有实现新的接触速度算法。

## 4. 明确保留的竖直环境速度问题

`BuildMotionFrame` 的着地分支已用 TODO 注释标注以下事实：

```text
原始竖直合速度 = verticalSpeed + phyVSpeed

空中：motionDelta.y 使用原始竖直合速度

着地：先以 verticalSpeed * dt 重建竖直位移
      再加地面切线运动
      不加入 phyVSpeed

平台：platformDelta.y 另行加入
```

`verticalSpeed` 含自研重力和跳跃累计，不应按字段分组把它理解成纯主动目标。`phyVSpeed` 与平台 Y 位移来自不同链路，平台位移不能替代被省略的竖直环境速度。

本次既不补加 `phyVSpeed`，也不清零它；只让预测遵循现有提交规则。调试窗口仍能通过“速度积分位移”和“斜坡修正后位移”的差异观察该情况。

后续必须单独核对这些场景，再决定是否以及如何修改规则：

1. 平地静止时，正向竖直环境速度是否应使实体离地。
2. 平地静止时，负向竖直环境速度是否应被地面约束抵消。
3. 起跳首帧几何仍报告着地时，环境速度如何参与。
4. 斜坡切线具有 Y 分量时，如何区分地面运动与竖直外部速度。
5. 升降平台与竖直环境速度同时存在时，二者如何组合。

这些是待决定的玩法/运动约束规则，不以当前行为等价检查代替策略验收。

## 5. 本轮验证及限制

- 使用项目现有 Unity Roslyn 响应文件完整编译运行时与编辑器程序集，均通过；验证输出写入忽略目录 `Temp/MotionSnapshotValidation`。
- 对实际编译的 `BasicEntity.BuildMotionFrame` 和 `EntityMotionFrame` 执行 23 项检查，覆盖空中正负环境速度、着地省略竖直环境速度、起跳首帧、15° 斜坡双向运动、相反坡向、平台补偿、不同步长及快照只读性。
- 数值检查确认着地规则按现状保留，不表示该规则已经被认定为正确设计。
- EditorOnly 格式整理后，重新通过运行时/编辑器程序集编译，并通过移除 `UNITY_EDITOR` 系列符号的运行时代码编译检查；该项用于验证观测字段隔离，不是完整 Player 构建。
- 尚未完成 Unity Play Mode 的斜坡推箱、升降平台和接触手感验收。预测一致性修复不等于接触轴、skin/释放阈值、多箱传播及 Unity 二次修正全部解决。

人工验收时观察“预测/提交共用基础位移”“接触对偏置”“静墙裁剪前位移”“最终请求位移”“引擎修正量”。先区分基础运动、接触纠偏与引擎修正，再判断剩余问题。

相关：[04 当前管线](./04-实体接触速度算法.md) · [05 数据语义](./05-数据语义与调试约定.md) · [06 开工事务](./06-已拍板事务与开工顺序.md)


---

## 6. F_5.2：环境能力与登记关系

环境组件保留原有场景身份和具体参数，公共接入由 BasicPhysicalObject、EnvironmentRegistration 与 EntityEnvironmentContext 协作完成。

| 结构 | 当前职责 |
|---|---|
| 能力接口 | 提供移速修饰、持续速度、动态施力、地面响应、平台位移、墙滑等可读取能力 |
| BasicPhysicalObject | Unity 环境入口、Registration 持有、公共生命周期转发 |
| EnvironmentRegistration | 源端反向索引、区域采样缓存和稳定来源身份；不保存累计速度 |
| EntityEnvironmentContext | 每个实体一份绑定账本，记录接入依据和已登记效果 |
| EntityEnvironmentFrame | 相位 2 的支撑参数快照；不持有跨帧动态速度 |

冰面、力场、水域不再分别维护实体进入/退出名单。区域关系由相位 2 重叠采样建立，多个 Collider 对同一实体取并集；最后一个接入依据消失时，Context 按登记凭据撤销效果。动态力退出沿用 fadeAway，已经获得的速度仍归实体并继续衰减。

支撑参数、平台位移和墙滑能力均从真实 Collider 对应的环境入口读取。无脚本地面仍可提供几何支撑，无墙滑接口的墙仍可阻挡。平台 Delta 在相位 2 采样并随运动快照计入一次，MovePosition 仍是相位 5 最后一步。

## 7. F_5.3：唯一环境入口（已完成）

F_5.3 在 F_5.2 的关系结构上进一步收窄环境入口：

- BasicPhysicalObject 标记为不可重复挂载；编辑器验证工具可发现同一物体的重复环境宿主。
- EnvironmentCapabilities 先确定唯一 BasicPhysicalObject 宿主，再从同一组件读取多个能力接口。
- 同一组件实现多个能力时仍只有一个 EnvironmentRegistration 和一个来源身份。
- EntityEnvironmentContext 的 groundSource 改为本次相位 2 脚下来源的单引用；bindings 仍保留多来源关系，以支持脚下、区域和显式入口同时存在。
- 区域多 Collider 去重、来源/实体启停、Collider 删除、重新接入和动态尾效规则保持。

本单元没有新增环境管理器、入口父类或通用效果容器，也没有替换接触速度算法和物理相位。

验收结果：Unity Edit Mode 环境集成检查 60 项通过；运行时与编辑器程序集编译、单宿主多接口读取、重复宿主检查、平台位移计入次数和生命周期清理均已验证。Play Mode 连续手感、接触抖动、多箱迭代仍属于后续事务。

代码阅读入口见 [环境接口抽象-开发对照.drawio](../../环境接口抽象-开发对照.drawio)，按 ENV-01 → ENV-07 阅读实体环境链路。

## 8. 当前保留规则与未决问题

着地分支省略竖直环境速度 phyVSpeed 的现有行为保持，代码已标注，尚未把“记录事实”和“修改处理规则”混在本阶段。后续需单独核对平地、起跳首帧、斜坡和升降平台组合。

F_5.4 已完成，落地见第 10 节。参数持续更新、通用效果数据结构和多箱速度协调不属于本阶段。

相关：[04-实体接触速度算法](./04-实体接触速度算法.md) · [05-数据语义与调试约定](./05-数据语义与调试约定.md) · [06-已拍板事务与开工顺序](./06-已拍板事务与开工顺序.md)

---

## 9. F_5.4 步骤评估（2026-09-16，开工前）

本节保留开工前现码事实；落地结果见第 10 节。

结构验证单元。公共入口应收成薄层：身份、`EnvironmentRegistration`、Unity 生命周期与接入调度。具体效果由能力接口声明。不新增入口父类或全局环境管理器。

原 9 步方向对，但不能按原文逐条执行：1/2/5/6 已能在评估里冻结；3 与 7 各自超过单步代码量；`IForceAction` 是实体写入协议，不是删除目标。清单缺 `pond` 类与 `Pond.prefab`（实际是 `ForceField`）、测试表面 `EnvironmentCheckSurface`，以及 `BasicEntity` 自身也实现了 `IPhyBaseI`。

### 现码事实（能力）

`RegisterEffects` 对已实现接口的零值也会登记。基类默认实现 `IMovementSpeedModifier` / `IStateVelocitySource`，因此所有环境入口都会写入移速修饰和持续速度。

| 类型 / 资源 | 真实能力 | 基类默认带来的项 | 资源值 |
|---|---|---|---|
| `IceGround` / `ICE.prefab` | 动态力、地面响应、移速修饰；公式读取 `speedChangeNum` | 持续速度（值为 0 仍登记） | `speedChangeNum=-0.5`，`slowingEffect=0.5`，`jumpHeightNum=-0.5` |
| `IceGround` / `ICE 1.prefab` | 同上，且持续速度为真值 | — | `phySpeed.x=3` |
| `ForceField` / `F1.prefab` | 动态力 `add/maxSpeed` | 移速、持续速度（均为 0） | `maxSpeed=10` |
| `ForceField` / `Pond.prefab` | 动态力 + 移速修饰 | 持续速度 0 | `speedChangeNum=-0.4`，`add=2`，`maxSpeed=5` |
| `Taijie` / `Resources/default/Taijie.prefab` | 平台位移、地面响应、移速修饰 | 持续速度 0 | `speedChangeNum=1`，`slowingEffect=4`，`jumpHeightNum=0.5` |
| `Wall` / `wall.prefab` | 墙滑 | 移速 0、持续速度 0、地面响应默认 | `speedChangeNum=0` |
| `pond` 类 | 区域入口外壳 | 会继承基类两项状态能力 | 场景未挂此类；水域资源是 `ForceField` |
| `EnvironmentCheckSurface` | 测试用平台 + 墙滑 + 基类持续速度 | 依赖基类 `phySpeed` | 代码里赋值 `(3,2)` |

归属冻结（有异议再改）：

- `IceGround`：移速、持续速度、动态力、地面响应。
- `ForceField`：动态力；移速修饰留下（`Pond.prefab` 的 `-0.4`）。
- `Taijie`：平台、地面响应；移速修饰留下（预制体 `1`，保留资源值）。
- `Wall`：只留墙滑。仍继承 `BaseGround` 时，踩墙顶可能建立无效果的 Ground 绑定；不在本单元改继承。
- `BaseGround`：只留 `IGroundResponse`。`Delta` 仅 `Taijie` 经 `IPlatformMotion` 消费。
- `BasicPhysicalObject`：不再默认提供移速/持续速度。
- `pond` 类不删；不按类名猜测 `Pond.prefab`。

`Wall` 上未使用的攀爬/墙跳字段、`BaseGround.Delta` 公开暴露，本单元不处理。

### 现码事实（旧协议）

| 符号 | 实际承担 | 替代 |
|---|---|---|
| `IApplyingForceAction` | 实体 `phyStateDic` / `startSpeedDic` 的键类型；`OnPhyEnter/Exit` | 键已是 `EnvironmentRegistration` 实例。生产代码无 `OnPhyEnter` 调用 |
| `IPhyBaseI` | 相位 1 写入 `nowtaijie`、几何 `canLeftWall/canRightWall`；`BasicEntity` 也实现了它 | 墙滑已走 `Collider` + `IWallSlideSurface`。`nowtaijie` 只写不读 |
| `IForceAction` | 实体效果容器协议；冰面/力场算法参数 | **保留**。只改来源键参数类型，不迁走写入口 |
| `Access.Explicit` | Context 第三种接入依据 | 生产无入口；编辑器检查用 `OnPhyEnter` 验证“区域消失仍保留显式依据” |

Explicit：保留 `Access.Explicit` 语义；测试改为 `EnvironmentRegistration` 窄口；再删 `IApplyingForceAction` 与基类转发。

### 执行步骤

一次只做一步。`F_5.4_1` 即本表，无异议后从 `_2` 开工。

| 步骤 | 做什么 | 验收 |
|---|---|---|
| `F_5.4_1` | 冻结上表 | 类型、资源、默认能力与真实能力已区分 |
| `F_5.4_2` | 基类去掉两项默认能力接口；需要者自行声明（含测试表面） | 未声明的类型不再登记移速/持续速度；已声明者仍按原值登记 |
| `F_5.4_3` | `speedChangeNum` / `phySpeed` 下移到已声明对应接口的类型；基类删除字段；不改公式 | 预制体 YAML 字段名仍对上；Ice 公式仍读同一序列化名 |
| `F_5.4_4` | 编译、预制体值、环境接入检查 | 能力缺席不再因 0 值登记；`ICE` / `Pond` / `Taijie` 数值保持。2026-09-17：运行时/编辑器 rsp 编译通过；Edit Mode **69** 项通过 |
| `F_5.4_5` | `phyStateDic`、`startSpeedDic` 与 `IForceAction` 对应参数改为 `EnvironmentRegistration` | 来源键、撤销、动态力键（仍是 `IDynamicAddForce`）语义不变 |
| `F_5.4_6` | `GeometricQuery`（角色与箱）不再 `TryGetComponent<IPhyBaseI>`；只保留 Collider | 墙滑与脚下接入仍走 Collider |
| `F_5.4_7` | 删除 `IPhyBaseI`、几何字段和只读包装暴露 | 编译通过；无脚本地面/墙几何不变 |
| `F_5.4_8` | Explicit 窄口替换 `OnPhyEnter`；删除 `IApplyingForceAction` 及基类转发 | 测试仍能验证第三种依据；生产无隐式 Enter |
| `F_5.4_9` | 资源值、缺席、生命周期、重复接入、尾效、编译、编辑器检查；更新开发对照图旧→新 | 2026-09-17：rsp 编译通过；Edit Mode **69** 项通过；对照图第 4 页 |

不处理：参数持续更新、通用环境效果容器、空间采样函数数组、入口父类、全局环境管理器、接触算法、`Wall` 是否继续作为脚下来源。

---

## 10. F_5.4：兼容职责退出（已完成）

2026-09-17 按第 9 节 `_2`–`_9` 落地。公共入口收成薄层：身份、`EnvironmentRegistration`、Unity 生命周期与接入调度。具体效果由能力接口声明。未新增入口父类或全局环境管理器。

| 类型 | 现声明能力 | 资源值保持 |
|---|---|---|
| `IceGround` | 动态力、地面响应、移速、持续速度 | `ICE` `speedChangeNum=-0.5`；`ICE 1` `phySpeed.x=3` |
| `ForceField` | 动态力、移速 | `Pond` `speedChangeNum=-0.4` |
| `Taijie` | 平台、地面响应、移速 | `speedChangeNum=1` |
| `Wall` | 墙滑 | 零值不再登记移速或持续速度 |
| `BaseGround` | 地面响应 | 无默认移速/持续速度 |
| `BasicPhysicalObject` | 仅 `IEnvironmentSource` | 无效果字段 |

来源键：`phyStateDic` / `startSpeedDic` 为 `EnvironmentRegistration`。动态力键仍是 `IDynamicAddForce`。几何查询只认 `Collider2D`。`Access.Explicit` 入口是 `SetExplicitAccess`。已删除 `IApplyingForceAction`、`IPhyBaseI`。`IForceAction` 保留。

- 运行时 / 编辑器 rsp 编译通过。
- Unity Edit Mode `EnvironmentIntegrationChecks.RunForBatch`：**69** 项通过（资源值、能力缺席、生命周期、重复接入、尾效、Explicit、单宿主约束）。
- 对照图：[环境接口抽象-开发对照.drawio](../环境接口抽象-开发对照.drawio) 第 4 页为旧→新映射；[环境接口抽象.drawio](../环境接口抽象.drawio) 能力归属与协议说明已同步。

阅读入口仍是第 3 页 ENV-01 → 07；能力现在落在具体组件接口上，不在基类默认实现里。不处理项仍按第 9 节。
